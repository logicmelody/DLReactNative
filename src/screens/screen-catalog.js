const PACKAGE_NAME = 'com.dl.dlreactnative';

const ScreenCatalog = {
	FIRST_SCREEN: `${PACKAGE_NAME}.FIRST_SCREEN`,
	SECOND_SCREEN: `${PACKAGE_NAME}.SECOND_SCREEN`,
};

export default ScreenCatalog;
